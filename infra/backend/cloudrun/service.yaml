apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: ${SERVICE_NAME}
  annotations:
    run.googleapis.com/ingress: all
spec:
  template:
    metadata:
      annotations:
        run.googleapis.com/cloudsql-instances: ${PROJECT_ID}:${REGION}:${CLOUD_SQL_INSTANCE}
    spec:
      containers:
        - image: ${REGION}-docker.pkg.dev/${PROJECT_ID}/${REPOSITORY}/backend:${IMAGE_TAG}
          ports:
            - containerPort: 3000
          env:
            - name: PORT
              value: "3000"
            - name: DB_NAME
              value: ${DB_NAME}
            - name: DB_USER
              value: ${DB_USER}
            - name: DB_SOCKET_PATH
              value: ${DB_SOCKET_PATH}
            - name: DB_INSTANCE_CONNECTION_NAME
              value: ${PROJECT_ID}:${REGION}:${CLOUD_SQL_INSTANCE}
            - name: FRONTEND_URL
              value: ${FRONTEND_URL}
            - name: FRONTEND_ORIGINS
              value: ${FRONTEND_ORIGINS}
            - name: GOOGLE_CALLBACK_URL
              value: ${GOOGLE_CALLBACK_URL}
            - name: GITHUB_CALLBACK_URL
              value: ${GITHUB_CALLBACK_URL}
            - name: GOOGLE_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  secret: ${GOOGLE_CLIENT_ID_SECRET}
                  version: ${GOOGLE_CLIENT_ID_VERSION}
            - name: GITHUB_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  secret: ${GITHUB_CLIENT_ID_SECRET}
                  version: ${GITHUB_CLIENT_ID_VERSION}
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  secret: ${DB_PASSWORD_SECRET}
                  version: ${DB_PASSWORD_VERSION}
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  secret: ${JWT_SECRET_SECRET}
                  version: ${JWT_SECRET_VERSION}
            - name: GOOGLE_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  secret: ${GOOGLE_CLIENT_SECRET_SECRET}
                  version: ${GOOGLE_CLIENT_SECRET_VERSION}
            - name: GITHUB_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  secret: ${GITHUB_CLIENT_SECRET_SECRET}
                  version: ${GITHUB_CLIENT_SECRET_VERSION}
            - name: OAUTH_STATE_SECRET
              valueFrom:
                secretKeyRef:
                  secret: ${OAUTH_STATE_SECRET_SECRET}
                  version: ${OAUTH_STATE_SECRET_VERSION}
